apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
data:
  grafana.ini: |
    [server]
    http_port = 3000

    [auth]
    disable_login_form = false

    [users]
    allow_sign_up = false
